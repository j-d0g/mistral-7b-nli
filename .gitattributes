# Disable Git LFS tracking for all files
*.png -filter -diff -merge -text
*.jpg -filter -diff -merge -text
*.jpeg -filter -diff -merge -text
*.webp -filter -diff -merge -text
*.gif -filter -diff -merge -text

# Set default behavior for line endings
* text=auto eol=lf

# Mark binary files explicitly
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
